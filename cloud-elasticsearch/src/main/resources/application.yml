# 数据源配置
spring:
  profiles:
    active: dev # 配置存放在nacos config cloud-elasticsearch-dev.yaml
  main:
    allow-bean-definition-overriding: true

logging:
  level:
    com.everyman: debug
    org.springframework: warn